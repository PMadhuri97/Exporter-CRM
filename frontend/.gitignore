node_modules/
dist/
dist-ssr/
*.local
.env
.env.local
coverage/
*.tsbuildinfo

# Regenerated by `npm run generate:api` — not hand-edited, but not checked in
# either, so a stale copy never silently drifts from the backend it was
# generated against. Run the script after pulling backend changes.
openapi.json
src/lib/api/schema.ts

*.log
npm-debug.log*
